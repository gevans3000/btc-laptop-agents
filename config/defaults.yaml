# Default Configuration for BTC Laptop Agents
# Loosely coupled defaults that can be overridden by env vars or strategy configs

trading:
  starting_balance: 10000.0
  risk_pct: 1.0
  stop_bps: 30.0
  tp_r: 1.5
  fees_bps: 2.0
  slip_bps: 0.5
  max_leverage: 20.0
  max_position_size_usd: 200000.0
  max_daily_loss_usd: 50.0

system:
  stale_timeout_sec: 120
  execution_latency_ms: 200
  log_level: INFO
