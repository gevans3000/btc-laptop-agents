# btc-alert-mvp

Minimal BTC alert pipeline with collectors, feature scoring, LLM summarization, Telegram notifier, and local anti-spam state.

## Run

```bash
pip install -e .
btc-alert run --interval 300 --symbol BTCUSDT
```
